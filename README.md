# hebrewfonts

```
sudo apt update && sudo apt install git && cd ~/Documents && git -c http.sslVerify=false clone https://github.com/lo-mityaesh/hebrewfonts.git && cd hebrewfonts && sudo cp *.ttf /usr/share/fonts/truetype/ && sudo fc-cache -f -v && cd .. && sudo rm -r hebrewfonts
```
